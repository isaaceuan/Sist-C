<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario  
            img(src='https://kurando.com.mx/wp-content/uploads/2019/06/kurando-logotipo.png')
 
            h1 Crea tu cuenta en Kurando

            if locals.mensajes   
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]    
                            .alerta(class=`${categoria}`)
                                p=error           
               
            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") Email:
                    input(type="email" name="email" placeholder="Tu email" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Tu Password" value= password)
                .campo2.enviar
                    input.boton(type="submit" value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a iniciar Sesion
        script(src="/dist/bundle.js")
    